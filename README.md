Copy and paste the [`config.example.json`] file and rename it to `config.jspn`, then fill it with the precise variables.

Once all development requirements are set up:
```bash
# Run in development mode:
$ yarn start
```